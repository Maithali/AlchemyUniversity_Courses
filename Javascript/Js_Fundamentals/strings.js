/*
 Your Goal: Hello World

In the variable a is stored the value "World". Your task is to store "Hello World" in the variable b.

    There's a couple ways to do this. If you want to test yourself, try interpolating the value a in b using backticks!
*/
const a = "World";
const b = "Hello World";
const helloMessage = `Hi ${a}, No Actually ${b}!`;

module.exports = { a, b };
